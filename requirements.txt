# This file lists the core dependencies for the Early Stage Diabetes Risk Prediction project.
# It's recommended to use these specific versions or newer compatible ones.
# You can install these dependencies using: pip install -r requirements.txt

pandas>=1.3.0
numpy>=1.20.0
scikit-learn>=1.0.0
imbalanced-learn>=0.8.0  # For SMOTE
joblib>=1.0.0
seaborn>=0.11.0
matplotlib>=3.4.0
scipy>=1.7.0
shap>=0.40.0

# Jupyter environment (optional, for running the notebook)
# jupyterlab>=3.0.0
# notebook>=6.0.0
# ipykernel>=6.0.0
